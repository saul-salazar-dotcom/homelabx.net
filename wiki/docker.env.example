# Outline Wiki Configuration
# Copy this file from the generated docker.env or create based on this template
# DO NOT commit docker.env files to version control

# –––––––––––––––– REQUIRED ––––––––––––––––

NODE_ENV=production

# Fix for localhost certificate (unable to verify certificate)
NODE_TLS_REJECT_UNAUTHORIZED=0

# Generate a hex-encoded 32-byte random key with: openssl rand -hex 32
# Or: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_hex_encoded_32_byte_key_here

# Generate a unique random key
UTILS_SECRET=your_hex_encoded_32_byte_key_here

# For production point these at your databases
DATABASE_URL=postgres://postgres:your_password@postgres:5432/outline
DATABASE_CONNECTION_POOL_MIN=
DATABASE_CONNECTION_POOL_MAX=
PGSSLMODE=disable

# For redis
REDIS_URL=redis://redis:6379

# URL should point to the fully qualified, publicly accessible URL
URL=http://wiki.homelab.com
PORT=3000

# Specify what storage system to use. Possible value is one of "s3" or "local".
FILE_STORAGE=local

# If "local" is configured for FILE_STORAGE
FILE_STORAGE_LOCAL_ROOT_DIR=/var/lib/outline/data

# Maximum allowed size for the uploaded attachment.
FILE_STORAGE_UPLOAD_MAX_SIZE=262144000
